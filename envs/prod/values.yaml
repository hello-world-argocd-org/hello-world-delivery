springboot-app:
  image:
    tag: "0.0.1-SNAPSHOT"
  ingress:
    hosts:
      - host: prod.hello-world.local
        paths:
          - path: /
            pathType: Prefix
  config:
    ENV_VALUE: PROD